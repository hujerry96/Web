---
import { SITE, withBase } from '../consts';
import { useTranslations } from '../i18n/ui';
import type { Locale } from '../i18n/config';

interface Props { locale: Locale; }
const { locale } = Astro.props;
const t = useTranslations(locale);
const year = new Date().getFullYear();
const cats = SITE.categories[locale];
---
<footer class="site-footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <span class="footer-logo">{SITE.name}</span>
        <p class="footer-desc">{SITE.description}</p>
      </div>
      <div class="footer-links">
        <span class="footer-label">{t('home.categories')}</span>
        {Object.entries(cats).map(([key, name]) => (
          <a href={withBase(`/${locale}/${key}`)}>{name}</a>
        ))}
      </div>
      <div class="footer-links">
        <span class="footer-label">{t('nav.about')}</span>
        <a href={withBase(`/${locale}/about`)}>{t('nav.about')}</a>
        <a href={withBase(`/${locale}/contact`)}>{t('nav.contact')}</a>
        <a href={withBase(`/${locale}/free-resources`)}>{t('nav.free')}</a>
        <a href={withBase(`/${locale}/rss.xml`)} class="rss">{t('rss.subscribe')}</a>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© {year} {SITE.name}. {t('footer.rights')}.</span>
      <span class="built">{t('footer.built')}</span>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--color-border);
    margin-top: var(--space-2xl);
    padding: var(--space-10) 0 var(--space-6);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    background: var(--color-surface);
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: var(--space-8);
    margin-bottom: var(--space-8);
  }
  @media (max-width: 640px) {
    .footer-grid { grid-template-columns: 1fr; gap: var(--space-6); }
  }
  .footer-brand { display: flex; flex-direction: column; gap: var(--space-2); }
  .footer-logo {
    font-family: var(--font-serif-display);
    font-weight: 700;
    font-size: 1.4rem;
    letter-spacing: 0.01em;
    color: var(--color-text);
  }
  .footer-desc { font-family: var(--font-serif-body); }
  .footer-desc {
    margin: 0;
    line-height: var(--line-height-normal);
    max-width: 24rem;
  }
  .footer-links {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }
  .footer-label {
    font-family: var(--font-serif-body);
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    font-size: 0.72rem;
    color: var(--color-accent);
    margin-bottom: var(--space-2);
  }
  .footer-links a {
    font-family: var(--font-serif-body);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease);
  }
  .footer-links a:hover { color: var(--color-accent); }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-2);
    padding-top: var(--space-6);
    border-top: 1px solid var(--color-border);
  }
  .built { opacity: 0.7; }
</style>
